export default function TransactionItem({name, className}: {name: string, className: string}) {
    return(
        <div className={`flex w-full flex-col h-fit ${className}`}>
            
        </div>
    )
}